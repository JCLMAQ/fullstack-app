/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.number(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().default(() => new Date()),
    pwdHash: z.string().nullish(),
    salt: z.string().nullish(),
    isAdmin: z.boolean().default(false).nullish(),
}
).strict();
const relationSchema = z.object({
    user: z.record(z.unknown()),
}
);
const fkSchema = z.object({
    userId: z.string(),
}
);

/**
 * `UserSecret` schema excluding foreign keys and relations.
 */
export const UserSecretScalarSchema = baseSchema.omit({
    pwdHash: true,
});


/**
 * `UserSecret` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const UserSecretSchema = UserSecretScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const UserSecretPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const UserSecretPrismaUpdateSchema = z.object({
    id: z.union([z.number(), z.record(z.unknown())]),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().default(() => new Date()),
    pwdHash: z.string().nullish(),
    salt: z.string().nullish(),
    isAdmin: z.boolean().default(false).nullish()
}).partial().passthrough();


/**
 * `UserSecret` schema for create operations excluding foreign keys and relations.
 */
export const UserSecretCreateScalarSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true, isAdmin: true
});


/**
 * `UserSecret` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const UserSecretCreateSchema = UserSecretCreateScalarSchema.merge(fkSchema);


/**
 * `UserSecret` schema for update operations excluding foreign keys and relations.
 */
export const UserSecretUpdateScalarSchema = baseSchema.partial();


/**
 * `UserSecret` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const UserSecretUpdateSchema = UserSecretUpdateScalarSchema.merge(fkSchema.partial());

