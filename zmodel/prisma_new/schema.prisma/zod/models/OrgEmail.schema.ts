/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.number(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().default(() => new Date()),
    published: z.boolean().default(true),
    isPublic: z.boolean().default(true),
    isDeleted: z.number().default(0),
    isDeletedDT: z.coerce.date().nullish(),
    email: z.string().email(),
    description: z.string().nullish(),
}
).strict();
const relationSchema = z.object({
    org: z.record(z.unknown()).optional(),
    OrgEmailUseTos: z.array(z.unknown()).optional(),
}
);
const fkSchema = z.object({
    orgId: z.string(),
}
);

/**
 * `OrgEmail` schema excluding foreign keys and relations.
 */
export const OrgEmailScalarSchema = baseSchema.omit({
    isDeleted: true,
});


/**
 * `OrgEmail` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const OrgEmailSchema = OrgEmailScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const OrgEmailPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const OrgEmailPrismaUpdateSchema = z.object({
    id: z.union([z.number(), z.record(z.unknown())]),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().default(() => new Date()),
    published: z.boolean().default(true),
    isPublic: z.boolean().default(true),
    isDeleted: z.union([z.number().default(0), z.record(z.unknown())]),
    isDeletedDT: z.coerce.date().nullish(),
    email: z.string().email(),
    description: z.string().nullish()
}).partial().passthrough();


/**
 * `OrgEmail` schema for create operations excluding foreign keys and relations.
 */
export const OrgEmailCreateScalarSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true, published: true, isPublic: true, isDeleted: true
});


/**
 * `OrgEmail` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const OrgEmailCreateSchema = OrgEmailCreateScalarSchema.merge(fkSchema);


/**
 * `OrgEmail` schema for update operations excluding foreign keys and relations.
 */
export const OrgEmailUpdateScalarSchema = baseSchema.partial();


/**
 * `OrgEmail` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const OrgEmailUpdateSchema = OrgEmailUpdateScalarSchema.merge(fkSchema.partial());

